extends /resources/angle/pug/edit

block title
  span(
  translate='backup.recurring.form.title.EDIT'
  translate-value-name='{{vm.edit.input.name}}'
  )

block form
  recurring-form(form="vm.edit")

block append right-side
  recurring-buttons(recurring="vm.edit.input")
